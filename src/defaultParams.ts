const AppParams = {
    minFrameLength: 40,
    maxChannelsCount: 45,
    defaultExportFileName: "program.bin",
}

export default AppParams;


export const ProgramSettings = {
    channelsCount: 8,
};